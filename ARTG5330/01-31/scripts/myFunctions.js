// A series of custom functions. This file can be considered
// as a custom library, and each of the functions below can be
// called from an html page after this js file has been loaded.

function sumAB(A, B) {

    return A + B;

}

function randomPosition() {

    return Math.random() * 140 + 70;
    
}